query GetRecipe($id: String!) {
  recipe(id: $id) {
    nodes {
      id
      name
      unit
      total
      depth {
        x
        y
      }
      node_type
    }
    edges {
      source
      target
    }
  }
}
